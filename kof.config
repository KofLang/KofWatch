# kof.config — gerado por kof config gen
# Chaves usadas pelo programa (em ordem de primeiro uso).
# Chaves com default estão comentadas — descomente para sobrescrever.

# db.url = jdbc:h2:./data/kofwatch;AUTO_SERVER=TRUE
# alerts.dir = alerts
# dashboards.dir = dashboards
# alerts.checkMs = 60000
alerts.checkMs = 5000
# retention.checkMs = 3600000
# retention.ms = 86400000
# server.port = 8080
